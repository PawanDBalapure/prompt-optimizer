# Optimize your first prompt

Two fast paths:

1. **Panel** - paste a prompt and press `Ctrl+Enter`.
2. **Editor/status bar** - select text and press `Ctrl+Alt+O`, or click
   **Optimize** in the status bar.

Optimize mode shows the analysis first. Agent mode submits the optimized prompt
to Copilot Chat.
