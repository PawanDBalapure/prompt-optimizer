# Optimize your first prompt

Two fast paths:

1. **From the panel** — paste a prompt and press `Ctrl+Enter`.
2. **From anywhere** — select text in any editor and press `Ctrl+Alt+O`
   (or click the **✨ Optimize** button in the status bar).  The optimized
   prompt is copied to your clipboard and prefilled into Copilot Chat.
