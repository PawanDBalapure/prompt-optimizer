# Open the Prompt Optimizer panel

Find **Prompt Optimizer** in the Activity Bar (sparkle icon) or press
`Ctrl+Alt+P` (`Cmd+Alt+P` on macOS).

The panel is where you paste prompts, run them through the optimizer, and see
the token / cost analysis.  All processing happens on your machine.
